# Always prefer setuptools over distutils
from setuptools import setup

setup(
    name     = 'tour it app',
    version  = '1.0.0',
    url      = 'https://github.com/vickichowder/vr-tour',
    keywords = 'Touring with a local in VR',

)
